{
  "name": "isolation",
  "version": "2.0.0",
  "private": true,
  "description": "A music library that composes itself as a night sky.",
  "scripts": {
    "ios": "cap add ios || true && cap sync ios && cap open ios",
    "sync": "cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^6.0.4",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/share": "^6.0.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
