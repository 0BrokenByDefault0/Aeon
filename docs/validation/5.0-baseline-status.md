# Aeon 5.0 baseline validation status

Task 1 is prepared as a checkpoint, not verified as a native release. Approved Aeon 4.6.1 web assets were restored, the iOS app target is set to 5.0 build 50, and an `AppTests` smoke target is wired through the shared CocoaPods workspace scheme. CI runs XCTest after Capacitor sync and pod installation and before packaging or artifact upload.

Local evidence:

- `npm ci --no-audit --no-fund`: passed with dependency warnings.
- `npm test`: 42 passed, 0 failed.
- `npm run check`: passed.
- `npm run test:browser` with recovered Chromium: 125 passed, 0 failed.
- `npm run test:interface` with recovered Chromium: failed the existing `no horizontal overflow at 390` assertion after its first five checks passed. Diagnostic-only runs identified the approved baseline’s `#nowStage::after` glow extending the scroll area; suppressing that pseudo-element clears the first assertion but then exposes the existing safe-area assertion (`player avoids home indicator at 844`, measured bottom 857.09 px). No UI or test was changed.
- `npm run test:arrival` with recovered Chromium: passed all 7 emitted checks.
- `npm run test:sky-chrome` with recovered Chromium: passed.
- `npm run test:native-audio` with recovered Chromium: passed.
- `xcodebuild -workspace ios/App/App.xcworkspace -scheme App -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test`: 85 tests executed, 0 failures, with the opt-in 48 kHz device-capture test skipped.
- Approved packaged executable SHA-256: `8f37f3ee7f5f1ddc1e680d336ed6548f544ed591141e6b5b51f575852044f646`.

Resume checkpoint on macOS:

- Tasks 2 through 5 are present: bridge-safe state/persistence, media access and probing, the transparent audio graph, and native queue/gapless scheduling.
- Task 6 adds `PlaybackCoordinator` as the serialized transport authority with generation-safe asynchronous loads, monotonic queue revisions and snapshots, persistence, interruption intent handling, and structured failures.
- Task 7 exposes that coordinator through the policy-free `NativeAudio` Capacitor plugin and the single `window.aeonTransport` native/browser adapter. Its command/event contract rejects stale native snapshots and does not resolve the browser backend in native mode.
- The complete iOS simulator suite now passes on iPhone 17 Pro / iOS 26.5. The bridge method list and coordinator event classification are covered by XCTest; the JavaScript bridge contract covers command forwarding, event registration, lazy browser fallback, and stale-event rejection.
- Tasks 8 through 14 have not started.
