# Physical corrective pass

Baseline: `codex/sky-ontology-reconstruction`, `b2f713a816e598ac5881b81ccc161d64a18228d8`. Continues the unmerged ontology line and its pending native-review repairs. No merge or output-rate matching.

- Sky: one album remains one core with a co-located corona. GPU flag 0x8000 explicitly identifies album instances; 0x800 identifies the unchanged deterministic backdrop. At broad scale album cores retain a point-space minimum and a nine-point corona radius. Artwork contributes 28% of stellar temperature. Playing and selected flags remain independent. Ambient particles never enter hit testing. Planets retain the floor(albumCount / 15) milestone model and a 20-point minimum diameter; surface frequencies fade with pixel footprint.
- Position: source frames are integer decoder frames. Scheduler position comes from source frames; snapshot position observes that authority. Display position clamps to duration. The exact visual EOF request is scheduled at file.frameCount - 1, never file.frameCount. Invalid negative/nonfinite/out-of-range requests remain errors. The timer observes state and never drives completion.
- Root cause: QueueScheduler.setRepeatMode previously captured position and rebuilt both players. Near EOF this could reprepare an EOF position; prepareItem rejected requested == frameCount. Repeat now changes future scheduling only. The coordinator responds to the scheduler's single completion event: One restarts frame zero, All wraps at the terminal boundary, Off stops. Output sample-rate reconfiguration remains deferred.
- Queue: UIKit interactive movement owns the local insertion preview, lifted row and edge scrolling. Cancellation commits nothing. One data-source move on release calls the same upcoming-only mutation as VoiceOver. QueueScheduler preserves the live current node and exact timeline; it retains an unchanged prepared successor and replaces only a changed alternate slot. Per-preparation identities reject obsolete callbacks without invalidating the current node.
- Album: artwork derives from container width and usable height, capped at 260 points and approximately 34% of vertical space. Ordinary serif titles use 35 points, long titles 32; accessibility text reflows. Play and Find in Sky are plain music actions, Add to Playlist secondary, Edit only in overflow. Cached artwork is retained.
- Validation: fast IPA is published before native tests. Native artifacts must include broad Sky, all Repeat states, queue reorder, ordinary/long album titles, compact/large/accessibility text, planet LOD and the ten-band EQ. Native simulator evidence is not a claim of physical-device acceptance.
