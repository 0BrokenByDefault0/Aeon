# Aeon 4.3

Native playback and arbitrary native folder selection remain pending. GitHub create-tree was retried during this pass and returned 403 Resource not accessible by integration; this Linux environment has no Xcode. This package does not contain a new native engine. A writable macOS Actions build or a local Xcode build is required before those features can be verified and delivered.

This checkpoint implements the independently authorized queue and search improvements:

- Drag or use arrow keys on the queue handle to reorder without replacing/restarting the audio source. Queue positions track the exact current occurrence even when the same song appears more than once.
- Save the complete queue as an independent named playlist. Clear upcoming entries while preserving the current track, and remove noncurrent tracks. Touch/pointer swipe-left removal is also wired; physical iPhone gesture validation remains outstanding.
- Correct restoration of the current duplicate queue occurrence, including backup queue sanitization.
- Direct song search results alongside albums, matching titles, track/album artists and genres with accent-insensitive tokens. Results render 40 songs at a time; a result plays that song in its album context or opens its track options.
- Clear search cancels queued debounce work. Queries cannot falsely match tokens split across unrelated song titles.

Validation: 42 unit tests, 125 browser regressions, 21 interface checks; visual inspection of queue/search. Real browser audio verified to keep playing through reorder, save, remove and clear operations. Native code and physical device behavior are not tested in this pass.

Packaged from the existing unsigned native shell, updating only web assets and version 4.3/build 43. Bundle app.isolation.sky and native executable/frameworks preserved. Cumulative patch base 849a1dbbdf66b37491964b54470241920fc6d9e3. Arthemys import, the airlock and planet art remain.
